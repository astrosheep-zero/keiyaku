---
id: task/usability/settle-verification-behavior-108f
title: Implement isolated Verification environments and observable cancellable execution
state: done
priority: 1
needs: []
parent: task/usability/follow-up-on-the-pi-flash-d8e2
supersedes: []
relates: []
note: ""
createdAt: 2026-09-09T12:54:16.998Z
updatedAt: 2026-09-10T01:25:07.663Z
---
Awaiting a coherent design and user decision. Do not implement the first consultation response as settled law.

User feedback: recurring agent failures around Verification are not solved by help text alone and conflict with SOUL.md. The problem includes rebuilding environments, long waits with no feedback, and unclear errors. The current trial reproduced worktree npm test passing while scratch Verification failed with xo: command not found; it did not reproduce a permanent hang. Source inspection additionally confirms timeout/cancellation return only a stop kind and discard already-collected output tails.

The observer withdrew the suggestion to default to the worker's existing worktree: it was not a complete solution and must not be treated as a decision. The current owner-law requirement for a disposable snapshot environment is being questioned, not preemptively reaffirmed or removed.

Discussion is with user-designated external participant pi-01a086315c49 in .square/KEIYAKU.square. Its first response proposes managed provisioning and a reusable verification worktree or small pool, keyed by snapshot-derived setup inputs. Follow-up fact gathering covers early acceptance emission, snapshot hooks, per-declaration observation, deadlines, concurrent verification, and ambient environment use. Those questions are being investigated. No final design has been accepted. In particular a persistent environment pool, cache key, bind-time preflight, default execution deadline, and durable per-declaration record model are proposals only.

Next planning outcome: answer the factual questions, examine the proposal against the observed failures and SOUL, return the coherent tradeoffs to the user, and only then split approved implementation work. Do not turn exit 127 or arbitrary output text into inferred environment authority, impose a new mandatory bind-time test ritual, silently mutate an active worker checkout, or present tests of one candidate as evidence for different bytes without an explicit reviewed design.

Consultation packet: /private/tmp/keiyaku-pi-flash-ux.iAwdzr/briefs/faye-verification-consultation.txt. Runtime evidence and full trial report are in the same lab directory. Read the owner index for verification, process runtime, lifecycle, public results, Git, settings, observation, and CLI before a coherent implementation change.
Consultation completed: .square/KEIYAKU.square act/1405 by the user-designated external participant pi-01a086315c49. Full answer: /private/tmp/keiyaku-pi-flash-ux.iAwdzr/evidence/verification-consultation-final.txt. Follow-up source packet: briefs/verification-consultation-followup.txt in the same lab. This supersedes the earlier waiting-for-external-answer note; state remains on_hold pending user decisions, not missing architect input.

The participant revised the initial proposal after the six factual answers. Existing snapshot-selected create/destroy hooks already provide setup. Whole lead durations do not establish setup cost. Generic shell exit 127 is not typed environment failure. Hashing generic hook text plus lockfiles does not establish safe environment reuse authority. Managed reuse and bind-time preflight are not approved implementation work.

Recommended first work: retain bounded captured output on stopped process outcomes; project accepted facts before awaiting trailing work; expose bounded Verification output during execution; make actual snapshot environment/setup facts and the existing provisioning entry point discoverable. Early admission alone does not expose declaration progress or output: these are separate changes. Preserve truthful exit classification and independent admission, evidence, and cleanup outcomes. Bind/amend observations must not claim knowledge of an unformed future delivery snapshot. Review owning public-result/CLI/Verification law coherently before implementing presentation and streaming; the external classification as no-new-law is not a substitute for that review.

Explicit user decisions still pending: change omitted declaration deadline to a project-configurable bounded default with an explicit unbounded option; overturn the current refusal of durable per-declaration start/end records. No specific default duration, output transport, storage layout, or execution-record lifecycle has been decided. Environment reuse is deferred pending isolated setup/run timing and a separately reviewed provisioning authority model. No implementation or commissioning has begun.
The user rejected the observation-first answer as insufficient: it described the failure without removing the environment assembly burden. Follow-up consultation act/1407 was fact-checked; revised act/1409 proposes substantive default provisioning, not yet approved. Proposal: Verification owns a bounded npm dependency-preparation step in each isolated candidate scratch when no project override applies; custom project setup remains available. This aims to remove the missing-dependency failure/amend/redeliver loop without using the worker checkout. It does not claim to eliminate network/setup failures or reduce measured installation time. Cache/pool/shared installed state proposals were withdrawn again: neither a lockfile key nor a typed npm branch establishes safe reuse.

Unsettled policies include whether to ship opinionated automatic provisioning, explicit override/disable semantics, lifecycle-script handling, effective dev-dependency selection under npm configuration (clearing NODE_ENV alone is not a complete guarantee), and finite default declaration deadlines. Audit prepares a prospective candidate from the current appointment/target, not a guaranteed rerun of the admitted snapshot; do not promise that recovery path. Changes to snapshot settings change the candidate. Sources: .square/KEIYAKU.square act/1409, src/protocol/audit.ts, npm ci documentation, and briefs/verification-provisioning-factcheck.txt in the trial lab. Current proposal artifact: evidence/verification-provisioning-proposal.txt. State remains on_hold; no implementation authorized.
User decision: rejected automatic ecosystem recognition/default provisioning as heuristic. A lockfile presence test does not establish or authorize the project environment preparation policy. Withdraw the recognized-npm/default-install proposal; do not implement it or disguise it as a default with an override. Earlier consultation recommendations are discussion history, not approved work. No replacement design is settled. The relationship between exact tested-content identity and the choice/creation of execution environment remains an unresolved architecture question. Keep this Task on_hold.
User requested continued architecture work rather than stopping without a conclusion. The premise-reopening packet was delivered to pi-01a086315c49; no answer arrived during two bounded waits totaling eight minutes or the following immediate catch. Do not attribute a further ruling to that participant.

Observer-owned research direction, NOT approved: separate the exact candidate tracked tree from an explicitly selected existing execution environment. Consider an isolated per-attempt copy of non-tracked environment state from the delivery workspace, with candidate tracked content authoritative, rather than ecosystem inference or mutation of the worker checkout. This deliberately challenges the current ban on inherited installed dependencies; it is not a claim that these dependencies were provisioned from the candidate lockfile or that changing source settings refreshes an admitted snapshot. No shared mutable installation tree or automatic install policy is implied.

A synthetic regular-file experiment succeeded: worker X tested feature=2,target=10,environment=3; bare integrated scratch lacked the fixture dependency; the same candidate with a separate copy of the ignored fixture dependency tested feature=2,target=20,environment=3. Writing the copied dependency did not change the source; candidate tracked bytes matched its Git snapshot; the source HEAD and tracked status were unchanged. Script: /private/tmp/keiyaku-pi-flash-ux.iAwdzr/probe-environment-transfer.mjs. Evidence: evidence/environment-transfer-probe.json in that lab. This only establishes the narrow content/environment separation example. It does NOT validate a generic copy implementation, live source consistency, symlinks, runtime metadata, relocation of arbitrary environments, platform portability, dependency freshness, or performance. Any implementation must explicitly resolve these ownership boundaries and fail rather than silently modifying the source. Task remains on_hold pending a reviewed and approved product decision. Keiyaku source is untouched.
User-confirmed requirement: intermediate observability must not be lost while redesigning the environment behavior. Treat environment usability and in-flight visibility as joint requirements, not alternative proposals. Accepted delivery must become visible before trailing work finishes. During execution expose observed phase transitions for candidate/environment preparation, declarations, and cleanup, with the addressed candidate and actual environment source/directory identifiable. Make command output available during the wait with bounded presentation, and preserve captured tails on failures, timeout, cancellation, and unknown exit. Distinguish accepted delivery, environment failure, test verdict, and cleanup failure. Quiet output, elapsed time, or a recorded start without an end must not be represented as proof of a live or hung process.

This requirement does not itself approve the observer environment-copy design, a durable execution-record model, a particular streaming/log transport, or default timeout values. Those implementation and policy boundaries remain to be settled. Do not regress to a final-only summary or frame visibility as dispensable because the environment is improved.
User authorized implementation after settling AsyncIterable progress with an independent final Promise and AbortController/AbortSignal cancellation. Implement eager execution independent of progress iteration; break unsubscribes, abort requests process cancellation with admitted receipts and cleanup retained. Integrate environment inheritance, runtime tails/output, local and forwarded public execution, CLI edge rendering, and tests. No heuristic package-manager installation, shared mutable dependency pool, default timeout change, or durable execution timeline. Worker-a only; preserve unrelated files. Planning history above is superseded only by this explicit approved scope.
Current-target integration against a03176f conflicted only at src/cli/invoke.ts. Keiyaku materialized preserved handoff f1e0c576; deliverer resolved worktree bytes preserving signal/progress and landed workdir/repo semantics, leaving real index UU intentionally. Final focused/typecheck/build/diff-check passed. Full npm test failed in facade-fleet.test.ts:662 ENOTEMPTY; no retries to seek green or unrelated teardown edits. Candidate frozen with invoke blob 5e122653b9ed39485c69aa0fc67e0f8131617d42; full inventory evidence/verification-integration-resolution-inventory-final.log. Independent current-target re-review and narrow read-only facade writer diagnosis commissioned. Main remains a03176f; no Verification placement.
COMPLETED: placed exact integration 5d1a6d0a27f7502e221dd2be8884ee7d5d89803c over a03176f with independent final candidate and exact-integration reviews satisfied, and all four integration Verification declarations satisfied. 49 paths, binary delta SHA256 47ae49efee26330b91b2f965ca77b5afbf29afb675705b11c5f5f9e542798268; formerly untracked additions captured. Resolved workdir/signal integration; candidate local CLI test now removes ambient AKUMA_REQUESTS and owns timeout child cleanup. Receipts: evidence/verification-delivery-resolved.json, verification-final-review.json; exact testimony review-verification-final-integration-verdict.txt. Separate facade/plugin cleanup risks and original direct-birth running unknown remain in investigation task, not claimed fixed by this green result.